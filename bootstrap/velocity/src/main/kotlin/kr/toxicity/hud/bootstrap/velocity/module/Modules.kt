package kr.toxicity.hud.bootstrap.velocity.module

import kr.toxicity.hud.bootstrap.velocity.module.velocity.VelocityStandardModule

val MODULE_VELOCITY = mapOf(
    "standard" to {
        VelocityStandardModule()
    }
)